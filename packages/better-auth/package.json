{
	"name": "@repo/better-auth",
	"version": "0.0.0",
	"private": true,
	"exports": {
		".": "./src/auth.ts",
		"./client": "./src/client.ts"
	},
	"scripts": {
		"test": "vitest run",
		"lint": "biome check",
		"lint:fix": "biome check --fix --unsafe",
		"schema:generate": "better-auth generate -y --output=../drizzle/src/schema/auth.ts"
	},
	"dependencies": {
		"@repo/drizzle": "workspace:*",
		"better-auth": "^1.4.18"
	},
	"devDependencies": {
		"@better-auth/cli": "^1.4.18",
		"@repo/biome": "workspace:*",
		"@repo/tsconfig": "workspace:*",
		"@repo/vitest": "workspace:*"
	}
}
