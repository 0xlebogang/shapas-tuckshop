{
	"name": "backend",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "air",
		"build": "go build -o ./build/main ./cmd/main",
		"test": "go test ./...",
		"lint": "golangci-lint ./...",
		"migrate": "go run ./cmd/migrations",
		"install": "go mod tidy"
	},
	"devDependencies": {
		"@repo/biome": "workspace:*"
	}
}
