# The port where the API server will listen for requests
PORT=

# The connection string to the database the API server will communicate with
DATABASE_URL=

# JWT Secret (use the command below to generate one or run the helper script
# from the scripts directory)
#
# node -e "console.log(require('node:crypto').randomBytes(16).toString('hex'))"
JWT_SECRET=
