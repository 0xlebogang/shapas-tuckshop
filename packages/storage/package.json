{
	"name": "@repo/storage",
	"version": "0.0.0",
	"private": true,
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"test": "vitest run",
		"lint": "biome check",
		"lint:fix": "biome check --fix --unsafe",
		"storage:start": "dotenv -c -- docker compose up -d",
		"storage:stop": "dotenv -c -- docker compose down -v"
	},
	"devDependencies": {
		"@repo/biome": "workspace:*",
		"@repo/tsconfig": "workspace:*",
		"@repo/vitest": "workspace:*"
	}
}
